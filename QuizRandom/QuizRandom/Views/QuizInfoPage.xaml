<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:QuizRandom.ViewModels"
             x:Class="QuizRandom.Views.QuizInfoPage">
    <ContentPage.BindingContext>
        <local:QuizInfoViewModel />
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <Grid Margin="{StaticResource PageMargin}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="4*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Label Grid.Row="0"
                   Text="{Binding QuizID, StringFormat='Quiz ID: {0}'}" />
            <Label Grid.Row="1"
                   Text="{Binding BestResultInfo}"
                   FontAttributes="Bold"
                   HorizontalOptions="CenterAndExpand"
                   VerticalOptions="CenterAndExpand" />
            <Button Grid.Row="2"
                    Text="Play"
                    Command="{Binding PlayQuizCommand}" />
            <Button Grid.Row="3"
                    Text="Delete"
                    Command="{Binding DeleteQuizCommand}" />
        </Grid>
    </ContentPage.Content>
</ContentPage>